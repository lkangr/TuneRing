using UnityEngine;
using UnityEngine.U2D;

public class HitSlider : MonoBehaviour
{
    private void Start()
    {
        Spline spline = new Spline();

        var line = GetComponent<SpriteShapeController>();
    }
}
